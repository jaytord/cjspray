package cfm.utils.youtube.ns
{
	/** The default namespace for YouTube feeds. 
	 * 
	 * <p> For elements that appear without a prefix (in the feed's XML) 
	 * the "atom::" prefix must be added to the element's name to be 
	 * properly referenced.</p>
	 */
	public namespace atom = "http://www.w3.org/2005/Atom"; 
}